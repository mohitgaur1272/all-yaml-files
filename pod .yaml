kubernetes apiVersion: v1
kind: Pod
metadata:
  name: merapod 
  labels:
    name: merapod 
spec:
  containers:
  - name: myapp
    image: httpd 
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: <Port>
