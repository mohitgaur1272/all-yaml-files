apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: merafirstpvc  
spec:
  resources:
    requests:
      storage: 500Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: fast
